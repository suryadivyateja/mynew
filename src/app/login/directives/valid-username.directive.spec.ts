import { ValidUsernameDirective } from './valid-username.directive';

describe('ValidUsernameDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidUsernameDirective();
    expect(directive).toBeTruthy();
  });
});
