import { ValidUserpasswordDirective } from './valid-userpassword.directive';

describe('ValidUserpasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidUserpasswordDirective();
    expect(directive).toBeTruthy();
  });
});
